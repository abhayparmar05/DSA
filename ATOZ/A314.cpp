#include <iostream>
using namespace std;

int main()
{
    int arr[10];
    for (int i = 0; i < arr.; i++)
    {
        arr[i] = (int)(Math.random() * 100);
        cout<<arr[i]<<" ";
    }

    set<int> st;

    for (int i = 0; i < arr.length; i++)
    {
        st.insert(arr[i]);
    }
    int index = 0;
    for (auto it : set)
    {
        arr[index] = it;
        index++;
    }
    return 0 ;
}
